public class missingChar {

    public String missingChar(String str, int n) {

        int længde = str.length();

        String myString = str.substring(3);
        myString = str.substring (2, 5);
        char bogstav = str.charAt(n);

        return "";

    }

}
